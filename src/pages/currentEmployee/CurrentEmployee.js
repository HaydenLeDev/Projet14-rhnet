import Table from "../../components/table/Table"

/**
 * Returns the employee table page
 * @returns Table
 */
const CurrentEmployee = () => {

    return (
        <main className="main_table">
            <Table />
        </main>
    )
}

export default CurrentEmployee
